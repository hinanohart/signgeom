{
  "name": "signgeom-web",
  "private": true,
  "version": "0.1.3",
  "type": "module",
  "description": "WebGPU + Canvas2D demo for signgeom (browser WebGPU API + WGSL).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@webgpu/types": "^0.1.50",
    "typescript": "^5.6.3",
    "vite": "^8.0.16"
  }
}
